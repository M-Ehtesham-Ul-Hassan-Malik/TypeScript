let magicanNames: string[] = ["Ahmed", "Naveed", "Qasim"];

function show_magican(magicanNames: string[]) {
    magicanNames.forEach(magican => {
        console.log(magican);
    });
}

show_magican(magicanNames);
